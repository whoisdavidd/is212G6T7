# Use an official Python runtime as a parent image
FROM python:3.10-slim

# Set environment variable to ensure output is flushed immediately
ENV PYTHONUNBUFFERED=1

# Set the working directory in the container
WORKDIR /usr/src/app

# Copy requirements file first to leverage Docker caching
COPY requirements.txt ./

# Install Python dependencies from requirements.txt
RUN pip install --no-cache-dir -r requirements.txt

# Copy the current directory contents into the container
COPY . .

# Expose the Flask port (default is 5005)
EXPOSE 5005  

# Define the command to start both the approval service and the worker
CMD ["sh", "-c", "python approval_service.py & python notification.py"]

# Use an official Python runtime as a parent image
FROM python:3.10-slim

# Set environment variable to ensure output is flushed immediately
ENV PYTHONUNBUFFERED 1

# Set the working directory in the container
WORKDIR /app

# Copy the current directory contents into the container at /app
COPY . /app

# Install gcc and other dependencies for building python packages like psycopg2 (or other DB connectors)
RUN apt-get update \
    && apt-get install -y gcc python3-dev libpq-dev \
    && apt-get clean

# Install any Python dependencies from requirements.txt (you might need to create this file if you don't have it)
COPY requirements.txt /app/requirements.txt
RUN pip install --no-cache-dir -r requirements.txt

# Make sure to expose the port your Flask app runs on (default Flask port is 5000)
EXPOSE 5000

# Copy the .env file
COPY .env /app/.env

# Define the command to start the Flask app
CMD ["python", "app.py"]
